# Repository Instructions

- Use `uv`, not `pip`, for environment and package management.
- Prefer `uv sync` to install project dependencies.
- Prefer `uv run ...` for commands such as tests, scripts, and ad hoc Python execution.
- If you need an editable install, use `uv pip install -e .`.
